apiVersion: v1
kind: Service
metadata:
  name: terralegislativepulse
  labels:
    app: terralegislativepulse
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: terralegislativepulse 